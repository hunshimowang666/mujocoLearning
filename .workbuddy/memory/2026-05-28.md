# 2026-05-28

## 修复 ESC 退出问题 (08_play_realtime.py)
- **问题**：按 ESC 关闭 MuJoCo 窗口后，GLFW 报 warning（不是 exception），导致 `try/except Exception` 没捕获到，模拟继续跑。
- **修复**：加 `warnings.filterwarnings("error", message=".*GLFW.*")` 把 GLFW warning 升级为 exception。
