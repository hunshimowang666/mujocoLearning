# Go2的Actor观察空间47维：
base_ang_vel        3维   机身角速度
projected_gravity   3维   重力方向投影到机身坐标系
command             3维   速度指令：vx, vy, yaw_rate
phase               2维   步态相位：sin/cos
joint_pos          12维   12个关节相对默认角度
joint_vel          12维   12个关节相对速度
actions            12维   上一次动作
# Go2的Critic观测空间74维：
Actor 的 47维
base_lin_vel         3维   机身线速度
foot_height          4维   四只脚高度
foot_air_time        4维   四只脚腾空时间
foot_contact         4维   四只脚是否接触
foot_contact_forces 12维   四只脚接触力，每只脚3维
# 动作空间12维：
FR_hip, FR_thigh, FR_calf
FL_hip, FL_thigh, FL_calf
RR_hip, RR_thigh, RR_calf
RL_hip, RL_thigh, RL_calf
# 力矩范围
hip_joint   最大力矩 ±23.5 Nm
thigh_joint 最大力矩 ±23.5 Nm
calf_joint  最大力矩 ±45 Nm
# 正弦曲线路径重训，删除旧网络
cd /home/administrator/mujocoLearning/examples/unitree_rl_mjlab
unset PYTHONNOUSERSITE
export LD_LIBRARY_PATH=/usr/lib/wsl/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}
export WANDB_MODE=disabled
export MUJOCO_GL=egl

/home/administrator/mujocoLearning/venv/bin/python scripts/train_go2_sine.py \
  --lin-vel-x=0.3 \
  --ang-vel-z-amplitude=0.35 \
  --period=10.0 \
  --num-envs=4096 \
  --path-duration=20.0 \
  --delete-old-networks
# 续训
cd /home/administrator/mujocoLearning/examples/unitree_rl_mjlab
unset PYTHONNOUSERSITE
export LD_LIBRARY_PATH=/usr/lib/wsl/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}
export WANDB_MODE=disabled
export MUJOCO_GL=egl

/home/administrator/mujocoLearning/venv/bin/python scripts/train_go2_sine.py \
  --lin-vel-x=0.3 \
  --ang-vel-z-amplitude=0.35 \
  --period=10.0 \
  --num-envs=4096 \
  --resume-latest
# 展示
cd /home/administrator/mujocoLearning/examples/unitree_rl_mjlab
unset PYTHONNOUSERSITE
export LD_LIBRARY_PATH=/usr/lib/wsl/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}
export WANDB_MODE=disabled
export MUJOCO_GL=egl

CHECKPOINT=$(find logs/rsl_rl/go2_sine_velocity -name "model_*.pt" | sort -V | tail -n 1)

echo "$CHECKPOINT"

/home/administrator/mujocoLearning/venv/bin/python scripts/play.py Unitree-Go2-Flat \
  --checkpoint_file="$CHECKPOINT" \
  --num-envs=1 \
  --device=cuda:0 \
  --viewer=viser \
  --sine-lin-vel-x=0.3 \
  --sine-ang-vel-z-amplitude=0.35 \
  --sine-period=10.0

# 后续干啥，弄点机械臂，小车的强化学习
路径跟踪的效果不太好，不改了，尽量不在别人的代码上增加新功能。要弄就自己从头做！！！
它原来的代码可能更偏向机器人先不倒地。那这样的话怎么加路径跟踪的误差还真不好弄了。
20260601：训练，续训，展示的CLI命令可以不用了，现在的版本是直接点击按钮就行，主要设置都在代码里了。